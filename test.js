/** @param {NS} ns **/

export async function main(ns) {
    // let's try to calculate various values that we could potentially use to determine the optimal number of threads for hacking
    // actually, let's just do that in manager.js
}
